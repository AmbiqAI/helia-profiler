# `local-experiments/` — developer-local experiment outputs (untracked)

Use this folder for sweep CSVs, summary JSONs, plots, scratch notebooks,
and any other artifacts produced by scripts in
[`local-scripts/`](../local-scripts/).

Everything here is `.gitignore`d *except* this README.  Treat it as a
disposable scratch dir; nothing under it is shared between machines.
