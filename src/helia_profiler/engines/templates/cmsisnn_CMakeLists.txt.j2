{# CMakeLists.txt — NSX wrapper for ns-cmsis-nn (Ambiq CMSIS-NN fork) #}
# Auto-generated by heliaPROFILER — do not edit.
cmake_minimum_required(VERSION 3.24)

# Collect all C source files from the CMSIS-NN Source/ tree
file(GLOB_RECURSE _CMSIS_NN_SRCS
    "${CMAKE_CURRENT_LIST_DIR}/Source/*.c"
)

add_library(nsx_cmsis_nn STATIC ${_CMSIS_NN_SRCS})
set_target_properties(nsx_cmsis_nn PROPERTIES EXPORT_NAME cmsis_nn)
add_library(nsx::cmsis_nn ALIAS nsx_cmsis_nn)

target_include_directories(nsx_cmsis_nn PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_LIST_DIR}/Include>
)

# CMSIS-NN needs CMSIS-Core headers (arm_math_types.h etc.) from the SDK.
if(DEFINED NSX_BOARD_FLAGS_TARGET)
    target_link_libraries(nsx_cmsis_nn PUBLIC ${NSX_BOARD_FLAGS_TARGET})
endif()
if(TARGET nsx_soc_hal)
    target_link_libraries(nsx_cmsis_nn PRIVATE nsx_soc_hal)
endif()
