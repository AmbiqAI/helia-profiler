"""Support-bundle collection and redaction for ``hpx doctor --bundle``.

``support_bundle`` collects the already-redacted members and their manifest;
``redact`` is the deterministic scrubbing policy it applies before anything
reaches disk.
"""
