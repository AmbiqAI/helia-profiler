// hpx compile-check stub — declarations only, tracks template usage (#187)
//
// Mirrors nsx-ambiq-hal includes-api/nsx_ambiq_pmu.h (uint32_t returns).
#pragma once

#include <stdint.h>

static inline uint32_t nsx_ambiq_pmu_enable(void) { return 0U; }
static inline uint32_t nsx_ambiq_pmu_disable(void) { return 0U; }
