// hpx compile-check stub — declarations only, tracks template usage (#187)
#pragma once

#include <stdint.h>

static inline uint32_t am_hal_timer_start(uint32_t timer) {
    (void)timer;
    return 0U;
}

static inline uint32_t am_hal_timer_stop(uint32_t timer) {
    (void)timer;
    return 0U;
}
